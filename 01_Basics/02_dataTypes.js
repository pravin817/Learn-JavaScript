"use strict"; //treat all JS code as newer version

// alert(2+4) // we are using nodeJS, not browser

console.log(3 + 3); // Avoid this type because the code readablity should be high

console.log("Pravin Mhaske");

// Data types

var name = "Pravin";
var age = 22;
var isLoggedIn = true;

// Number  => 2 to power 53
// bigInt
// String => ""
// Boolean => true/false
// null => standalone value
// undefined =>
// Symbol => Unique

// Object and Array

// Typeof

console.log(typeof undefined);   // undefined
console.log(typeof null);        // object

/*
    Note : The typeof null is ---> Object,
                      undefined ---> undefined
*/
